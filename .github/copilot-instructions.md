# Mindweave workspace

- Vite + React + TypeScript application.
- Run `npm install` then `npm run dev` for local development.
- Build with `npm run build`.
- The mindmap source is a nested Markdown bullet outline.
- Keep the visual language dark, compact, and logic-chart oriented.
